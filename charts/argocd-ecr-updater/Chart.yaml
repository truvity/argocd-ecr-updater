apiVersion: v2
name: argocd-ecr-updater
version: 0.0.0-dev  # set by the release workflow
description: "ECR token refresher for ArgoCD repo-creds Secrets: hourly CronJob + PostSync seed hook"
appVersion: 0.0.0-dev  # set by the release workflow
